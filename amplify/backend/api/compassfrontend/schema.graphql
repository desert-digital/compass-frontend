type Event @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID
  body: String
  start: String 
  end: String
  item: String
  item_id: String
  status: String
}

type Vessel @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  name: String
}

type Contact @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  name: String
  email: String
}

type Action @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  status: Boolean
  description: String
  notes: String 
  duration: Int
}

type Step @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  activity: Action @hasOne
}

type CheckList @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  owner: Contact @hasOne
  steps: [Step] @hasMany
  start: AWSDate
  end: AWSDate 
}

type Workflow @model @auth(rules: [{ allow: private }]){
  id: ID!
  company: ID!
  owner: Contact @hasOne
  steps: [CheckList] @hasMany
  start: AWSDate
  end: AWSDate 
}